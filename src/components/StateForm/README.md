# StateForm

<!-- STORY -->

## Introduction

StateForm is an easy-to-use component.

## Usage

```javascript
import { StateForm } from 'some-package-name';
```

## Example use

```javascript
const myPage = props => {
  return (
    <main>
      <StateForm />
    </main>
  );
};
```

## Properties

- `className` - is the class name of the component

| propName  | propType | defaultValue | isRequired |
| --------- | -------- | ------------ | ---------- |
| className | string   | -            | -          |
