# CashFlowCalculator

<!-- STORY -->

## Introduction

CashFlowCalculator is an easy-to-use component.

## Usage

```javascript
import { CashFlowCalculator } from 'some-package-name';
```

## Example use

```javascript
const myPage = props => {
  return (
    <main>
      <CashFlowCalculator />
    </main>
  );
};
```

## Properties

- `className` - is the class name of the component

| propName  | propType | defaultValue | isRequired |
| --------- | -------- | ------------ | ---------- |
| className | string   | -            | -          |
